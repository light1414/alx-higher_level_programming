#!/usr/bin/python3
"""Prints True if the object is exactly an instance of the specified class
; else False.
"""


def is_same_class(obj, a_class):
    """It returns True if the object is exactly an instance"""
    return True if type(obj) == a_class otherwise False
